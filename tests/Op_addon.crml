//Library Op_addon
model Op_addon
	Operator [ Requirements ] fors alls Requirements R = and R;

	Operator [ ] 'stop simulation when' Requirement R 'becomes violated' = when (evaluate R == false) terminate;

end Op_addon;
