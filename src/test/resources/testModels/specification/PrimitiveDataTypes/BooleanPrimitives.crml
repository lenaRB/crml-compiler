model BooleanPrimitives is {

// Boolean definition with predefined values
Boolean b0 is true;
Boolean b1 is false;
Boolean b2 is undecided;
Boolean b3 is undefined;

//external Boolean b4;

// Logical operators
Boolean b5 is b1 and b2;
Boolean b6 is not b1;

// Temporal operators - need to extend functions
Boolean b8 is b1 * b2;	// Filter
Boolean b10 is b1 + b2 + b3;	// Accumulation

/** Left to test/implement

Name
Conjunction
Disjunction
Conjunction with event
Disjunction with event
Negation
Filter
Accumulation
Integration
Duration
Equality
If then else
At event


*/

};
